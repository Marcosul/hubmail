# HubMail API — copie para .env
PORT=3002
# Webmail (dev) ou produção, para CORS
APP_URL=http://localhost:3010

# Integração Vercel → Supabase (nomes "STORAGE_*" sugeridos pelo painel) — opcionais
# se preencheres SUPABASE_* e DATABASE_ abaixo.
# STORAGE_SUPABASE_URL=
# STORAGE_SUPABASE_SERVICE_ROLE_KEY=
# STORAGE_POSTGRES_PRISMA_URL=
# ... (vê a integração no dashboard da Vercel)

# Aliases (Nest/Prisma) — mesmo projecto que STORAGE_
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Prisma: pooler + direct (host db.<ref>.supabase.co, não pooler, para migrate)
DATABASE_URL=
DIRECT_URL=
